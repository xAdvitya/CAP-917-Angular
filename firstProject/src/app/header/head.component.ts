import { Component } from '@angular/core';

@Component({
    selector: 'header',
    templateUrl: './head.component.html',
    // styleUrls
})

export class Header {
    title = 'header component';
}
